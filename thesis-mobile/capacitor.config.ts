import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.berlynachievers.examscanner',
  appName: 'Berlyn Exam Scanner',
  webDir: 'www'
};

export default config;
